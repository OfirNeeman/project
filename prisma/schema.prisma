datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id       Int      @id @default(autoincrement())
  username String   @unique
  password String
  profile  String?  // נשמור JSON של UserProfile
  savedItems String? // נשמור JSON של ClothingItem[]
}
